{
    "name": "@coveops/copy-to-clipboard",
    "version": "1.2.3",
    "description": "Allows the user to copy a value to their clipboard.",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "coveops build CopyToClipboard --template typescript",
        "watch": "coveops build CopyToClipboard --template typescript --watch",
        "serve": "coveops serve",
        "precompile": "npm run build"
    },
    "author": "",
    "license": "Apache-2.0",
    "devDependencies": {
        "@coveops/cli": "^0.17.0"
    },
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "dependencies": {
        "@coveops/turbo-core": "^1.2.1",
        "coveo-search-ui": "^2.10089.0"
    }
}
